import { Box } from "@mui/material";

const LoginPage = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Box>Hi</Box>
    </Box>
  );
};

export default LoginPage;
